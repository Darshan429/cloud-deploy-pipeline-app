# Copy this file to terraform.tfvars and fill in your own values.
# terraform.tfvars is gitignored — never commit it, it can contain
# machine-specific / semi-sensitive values.

my_ip    = "223.237.160.40"      # get yours from https://checkip.amazonaws.com
key_name = "expense-app-key"    # must already exist in the AWS console, same region
